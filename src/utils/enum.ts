export enum USER_STATUS {
  ONLINE = 'ONLINE',
  OFFLINE = 'OFFLINE',
}

export enum MESSAGE_STATUS {
  SENT = 'SENT',
  SEEN = 'SEEN',
}

export enum CHANNEL_STATUS {
  PRIVATE = 'PRIVATE',
  PUBLIC = 'PUBLIC',
}
